<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="card shadow-sm">
        <div class="card-body">
          <h2 class="mb-4 text-center text-primary">🔍 Tìm kiếm Sản phẩm (Solr)</h2>
          <%= form_with url: search_results_path, method: :get, local: true, class: "mb-4" do |f| %>
            <div class="input-group">
              <%= f.text_field :q, value: params[:q], class: "form-control form-control-lg", placeholder: "Nhập tên sản phẩm..." %>
              <button class="btn btn-primary btn-lg" type="submit">Tìm kiếm</button>
            </div>
          <% end %>

          <% if defined?(@results) && @results.present? %>
            <h5 class="mt-4">Kết quả tìm kiếm:</h5>
            <div class="table-responsive">
              <table class="table table-striped table-hover align-middle mt-2">
                <thead class="table-light">
                  <tr>
                    <th>Mã</th>
                    <th>Tên</th>
                    <th>Dòng sản phẩm</th>
                    <th>Vendor</th>
                    <th>Giá</th>
                  </tr>
                </thead>
                <tbody>
                  <% @results.each do |product| %>
                    <tr>
                      <td><%= product['id'] %></td>
                      <td><%= Array(product['productName']).join(', ') %></td>
                      <td><%= Array(product['productLine']).join(', ') %></td>
                      <td><%= Array(product['productVendor']).join(', ') %></td>
                      <td><%= Array(product['buyPrice']).join(', ') %></td>
                    </tr>
                  <% end %>
                </tbody>
              </table>
            </div>
          <% elsif defined?(@results) %>
            <div class="alert alert-warning mt-4">Không tìm thấy sản phẩm phù hợp.</div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- Bootstrap 5 CDN for quick styling (nếu chưa có) -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"> 