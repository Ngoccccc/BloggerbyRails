<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="card shadow-sm">
        <div class="card-body text-center">
          <h2 class="mb-4 text-primary">🛠 Import textDescription từ Productlines lên Solr</h2>
          <% if notice %>
            <div class="alert alert-success"> <%= notice %> </div>
          <% end %>
          <%= button_to "Import textDescription lên Solr", import_productlines_text_description_path, method: :post, class: "btn btn-lg btn-success" %>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- Bootstrap 5 CDN for quick styling (nếu chưa có) -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"> 